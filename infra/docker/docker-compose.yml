# Local dev infrastructure (Implementation Guide Phase 0 Definition of Done:
# "docker-compose up starts local n8n reachable at localhost:5678").
# Supabase itself (Auth/Realtime) is a hosted/cloud dependency per SAD §3.2 —
# only Postgres + n8n are self-hosted locally for development.
name: ai-ops-manager

services:
  postgres:
    image: postgres:15-alpine
    container_name: aiom-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "54322:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # NOTE: packages/database/sql/*.sql (RLS policies) are deliberately NOT
      # mounted to /docker-entrypoint-initdb.d — those statements reference
      # tables (organizations, users, tasks, ...) that only exist after
      # Prisma has migrated the schema. Running them at container-init time
      # (before any migration) would fail against an empty database. They're
      # applied instead by `pnpm db:migrate` (see packages/database/scripts/
      # apply-sql.sh), which runs strictly after `prisma migrate deploy`.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  n8n:
    build:
      context: .
      dockerfile: Dockerfile.n8n
    container_name: aiom-n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      N8N_BASIC_AUTH_ACTIVE: "true"
      N8N_BASIC_AUTH_USER: ${N8N_BASIC_AUTH_USER:-admin}
      N8N_BASIC_AUTH_PASSWORD: ${N8N_BASIC_AUTH_PASSWORD:-change-me}
      N8N_HOST: localhost
      N8N_PORT: 5678
      N8N_PROTOCOL: http
      WEBHOOK_URL: http://localhost:5678/
      GENERIC_TIMEZONE: UTC
    volumes:
      - n8n_data:/home/node/.n8n
      - ../n8n/workflows:/home/node/workflows:ro
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
  n8n_data:
